#include <stdio.h>
/**
 * main- Determines if a number is positive, negative or zero
 */
int main ()
{ 
	char alphabet;
	for(alphabet = 'a';alphabet <= 'z';alphabet++)
	putchar(alphabet);
	putchar('\n');
	return (0);
}
