#include <stdio.h>
/**
 * main- Determines if a number is positive, negative or zero
int main ()
{ 
	char alphabet = 'a';
	while alphabet <= 'z')
	{
		putchar(alphabet);
		alphabet++;

	}
	putchar('\n');
	return (0);
}
